name: Release

on:
  push:
    branches:
      - main
    tags:
      - 'v*.*.*'  # Match semantic version tags like v1.2.3

jobs:
  test:
    uses: ./.github/workflows/test.yml  # Runs your test suite via reusable workflow

  release:
    needs: test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Install sbt
        uses: coursier/setup-action@v1
        with:
          apps: sbt

<<<<<<< Updated upstream
      - name: Build JAR
=======
      - name: Set platform classifier
        run: echo "env.classifier=${{ matrix.classifier }}" >> $GITHUB_ENV

      - name: Print env.classifier and resolved JavaFX libs
        run: sbt 'show Compile/fullClasspath'

      - name: Build fat JAR
>>>>>>> Stashed changes
        run: sbt assembly

      - name: Release to GitHub
        uses: softprops/action-gh-release@v2
        with:
          files: target/scala-*/sgp.jar  # Adjust path if needed
